fastapi==0.111.0
uvicorn[standard]==0.30.1
jinja2==3.1.4
python-multipart==0.0.9
httpx==0.27.0
python-dotenv==1.0.1
# On-device (offline) speech-to-text — powers voice with no internet:
vosk==0.3.45
# Optional — only needed if you enable real Google Cloud voice (else browser fallback is used):
# google-cloud-speech==2.26.0
# google-cloud-texttospeech==2.16.3
